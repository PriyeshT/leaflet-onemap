mixin onemap(spec)
  - spec = spec || {}
  - spec.__class = spec.__class || ''

  div(class=spec.__class)&attributes(attributes)
    .map-container
      #map
    .map-directions
      p Directions List
      table.directions-table
